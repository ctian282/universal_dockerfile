FROM ubuntu:20.04
ENV DEBIAN_FRONTEND=noninteractive


# Install a bunch of build shit.
RUN	apt-get update \
    && apt-get install -y \
    build-essential \
    cmake \
    git \
    sudo \
    unzip \
    emacs \
    wget \
    curl\
    time\
    bzip2\
    #lpcola env
